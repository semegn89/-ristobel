# Инструкции по размещению медиа-файлов

## 📹 Видео для Hero секции

1. Поместите видео в папку: `public/videos/`
2. Название файла: `hero-video.mp4`
3. Рекомендуемые параметры:
   - Формат: MP4 (H.264)
   - Разрешение: 1920x1080 (Full HD) или выше
   - Длительность: 10-30 секунд (будет зациклено)
   - Размер файла: до 10-15 МБ (для быстрой загрузки)

4. Постер (изображение-превью):
   - Поместите в: `public/images/hero-poster.jpg`
   - Разрешение: 1920x1080
   - Формат: JPG или WebP

## 🎨 Логотип

Текущие файлы логотипа:
- `logo.svg` - для светлого фона (темно-синий)
- `logo-white.svg` - для темного фона (белый)

### Чтобы заменить логотип:

1. Замените файлы в папке `public/`:
   - `logo.svg` → ваш логотип для светлого фона
   - `logo-white.svg` → ваш логотип для темного фона

2. Рекомендации:
   - Формат: SVG (предпочтительно) или PNG
   - Размер: минимум 200x60px
   - Для SVG: убедитесь, что цвета адаптируются (currentColor или fill="white"/fill="#0A192F")

### Структура папок:

```
public/
├── videos/
│   └── hero-video.mp4          ← Ваше видео здесь
├── images/
│   └── hero-poster.jpg          ← Постер для видео
├── logo.svg                     ← Логотип (светлый фон)
└── logo-white.svg               ← Логотип (темный фон)
```

## ✅ После размещения файлов

После того как вы разместите файлы:
- Видео автоматически появится в Hero секции
- Логотип автоматически обновится на сайте
- Перезапускать сервер не нужно (Next.js подхватит изменения)

